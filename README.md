# my-alien-dog

function setup() {
  createCanvas(400, 400, WEBGL);
}

function draw() {
  background(0, 50, 200);
     // backround (red,)
  circle(-100, 0, 75);

triangle (300,300,300,300)
  circle(150, 150, 0, 0)
  ellipse(0, 10, 150, 100 )
  
}